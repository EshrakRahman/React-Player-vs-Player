import React from 'react';

const PlayerOne = () => {
    return (
        <React.Fragment>
            <button className={'player-btn'}>Player One</button>
        </React.Fragment>
    );
};

export default PlayerOne;