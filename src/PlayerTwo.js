import React from 'react';

const PlayerTwo = () => {
    return (
        <React.Fragment>
            <button className={'player-btn'}>Player Two</button>
        </React.Fragment>
    );
};

export default PlayerTwo;